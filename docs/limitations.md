# Limitations

- The public datasets are aggregates and intentionally omit raw evidence.
- Provider naming depends on the private source data quality.
- GeoIP, ASN, and hosting classifications can be incomplete or stale.
- Small providers can have unstable scores because a small number of observations changes the aggregate profile.
- Anonymized hosting clusters are useful for structure analysis but cannot be used to identify specific operators.

