# Interpretation Guide

Use the datasets as directional research signals.

- `high` confidence means the aggregate footprint is broad enough to support stronger interpretation.
- `medium` confidence means the signal is useful but should be compared with adjacent metrics.
- `low` confidence means the public aggregate may be based on limited observations.

Provider scores should not be used as blocklists, security verdicts, legal claims, or real-time operational intelligence.

